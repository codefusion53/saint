# Core dependencies for market prediction with PyTorch
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0  # For reading Excel files
psutil>=5.9.0    # For aggressive process cleanup (kills zombie workers)

# Optional: for better performance
scikit-learn>=1.3.0  # For additional preprocessing if needed
matplotlib>=3.7.0    # For visualization

# 16-bit floating point support
# PyTorch 2.0+ has built-in float16 support
# NumPy 1.24+ supports float16 operations

